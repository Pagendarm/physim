//
// Object for Axis of rotation
// indicator
//

package general {
    // Enumeration for axis' of rotation
    object Rotate_Axis extends Enumeration {
        type Axis = Value
        val X = Value ("X-Axis")
        val Y = Value ("Y-Axis")
        val Z = Value ("Z-Axis")
    }
}
